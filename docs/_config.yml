title: UM Tesorería Report Service
description: Servidor de para la Generación de Reportes de UM Tesorería
theme: jekyll-theme-cayman

# Configuración importante para Jekyll
defaults:
  -
    scope:
      path: "" # aplica a todos los archivos
    values:
      layout: default

# Especificar que los archivos .md deben ser procesados
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Configurar la página principal
index_page: index.md 